package com.work.testworkspace.enumTest;

public enum Week {
    MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY;
    // 위의 각 enum Constant [열거형인 상수] 들의 ordinal 을 get 해보면, (마치 인덱스처럼) 0부터 1,2, ... 순서대로 설정되어 있다.
}
